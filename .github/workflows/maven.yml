name: Java CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: |
            supergraph/pom.xml
            pricing-subgraph/pom.xml
            products-subgraph/pom.xml

      - name: Test subgraphs and local project
        working-directory: supergraph
        run: make test

      - name: Start federated stack
        working-directory: supergraph
        run: make up

      - name: Run smoke tests
        working-directory: supergraph
        run: make smoke

      - name: Run federated E2E tests
        working-directory: supergraph
        run: make e2e

      - name: Stop federated stack
        if: always()
        working-directory: supergraph
        run: make down

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: products-subgraph/target/site/jacoco/jacoco.xml,pricing-subgraph/target/site/jacoco/jacoco.xml
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
